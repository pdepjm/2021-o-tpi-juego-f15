import wollok.game.*
import jugador.*
import pantallaDeCarga.*
import nivel.*
import soundProducer.*

program juego {
	game.cellSize(40)
	game.height(25)
	game.width(50)
	//pantallaDeCarga.configuracionInicial()
	game.boardGround("mapa.png") //meto el fondo
	nivel.configuracionInicial()
	game.start()
}